import { AllstyleDirective } from './allstyle.directive';

describe('AllstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new AllstyleDirective();
    expect(directive).toBeTruthy();
  });
});
